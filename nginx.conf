server
{
    listen 3000;
    server_name wellwin.top;
    index index.html index.htm default.htm default.html;
    root /www/wwwroot/wellwin.top_3000;
    include /www/server/panel/vhost/nginx/extension/wellwin.top_3000/*.conf;
    
    #CERT-APPLY-CHECK--START
    include /www/server/panel/vhost/nginx/well-known/wellwin.top_3000.conf;
    #CERT-APPLY-CHECK--END

    #SSL-START
    #error_page 404/404.html;
    #SSL-END

    #ERROR-PAGE-START
    #error_page 404 /404.html;
    #error_page 502 /502.html;
    #ERROR-PAGE-END

    # ---------- 修改部分 START ----------
    # DNS 解析器（使用公共 DNS，宝塔 nginx 默认可能没配置 resolver）
    resolver 119.29.29.29 223.5.5.5 valid=300s;
    resolver_timeout 5s;

    # 天天基金接口反向代理
    location /api-fund/ {
        # 使用变量方式避免 nginx 启动时缓存 DNS
        set $fund_backend "fund.eastmoney.com";
        proxy_pass http://$fund_backend/;
        proxy_set_header Host fund.eastmoney.com;
        proxy_set_header Referer http://fund.eastmoney.com/;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header User-Agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36";
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }

    # 基金历史数据 JSONP 代理（原来 //fund.eastmoney.com/pingzhongdata/xxx.js）
    location /api-fund-jsonp/ {
        set $fund_backend "fund.eastmoney.com";
        proxy_pass http://$fund_backend/pingzhongdata/;
        proxy_set_header Host fund.eastmoney.com;
        proxy_set_header Referer http://fund.eastmoney.com/;
        proxy_set_header User-Agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36";
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }

    # 基金估值 JSONP 代理（原来 //fundgz.1234567.com.cn/js/xxx.js）
    location /api-estimate/ {
        set $gz_backend "fundgz.1234567.com.cn";
        proxy_pass http://$gz_backend/js/;
        proxy_set_header Host fundgz.1234567.com.cn;
        proxy_set_header Referer http://fundgz.1234567.com.cn/;
        proxy_set_header User-Agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36";
        proxy_connect_timeout 5s;
        proxy_read_timeout 15s;
    }

    # Vue History 模式支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    # ---------- 修改部分 END ----------

    # 禁止访问的敏感文件
    location ~* (\.user.ini|\.htaccess|\.htpasswd|\.env.*|\.project|\.bashrc|\.bash_profile|\.bash_logout|\.DS_Store|\.gitignore|\.gitattributes|LICENSE|README\.md|CLAUDE\.md|CHANGELOG\.md|CHANGELOG|CONTRIBUTING\.md|TODO\.md|FAQ\.md|composer\.json|composer\.lock|package(-lock)?\.json|yarn\.lock|pnpm-lock\.yaml|\.\w+~|\.swp|\.swo|\.bak(up)?|\.old|\.tmp|\.temp|\.log|\.sql(\.gz)?|docker-compose\.yml|docker\.env|Dockerfile|\.csproj|\.sln|Cargo\.toml|Cargo\.lock|go\.mod|go\.sum|phpunit\.xml|phpunit\.xml|pom\.xml|build\.gradl|pyproject\.toml|requirements\.txt|application(-\w+)?\.(ya?ml|properties))$
    {
        return 404;
    }

    # 禁止访问的敏感目录
    location ~* /(\.git|\.svn|\.bzr|\.vscode|\.claude|\.idea|\.ssh|\.github|\.npm|\.yarn|\.pnpm|\.cache|\.husky|\.turbo|\.next|\.nuxt|node_modules|runtime)/ {
        return 404;
    }

    # 一键申请SSL证书验证目录相关设置
    location ~ \.well-known{
        allow all;
    }

    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }

    location ~ .*\\.(gif|jpg|jpeg|png|bmp|swf)$
    {
        expires      30d;
        error_log /dev/null;
        access_log /dev/null;
    }

    location ~ .*\\.(js|css)?$
    {
        expires      12h;
        error_log /dev/null;
        access_log /dev/null;
    }
    
    access_log  /www/wwwlogs/wellwin.top_3000.log;
    error_log  /www/wwwlogs/wellwin.top_3000.error.log;
}
